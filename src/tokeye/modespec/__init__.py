"""Mode-number analysis suite (the modespec family).

- :mod:`tokeye.modespec.classic` — vendored ``pymodespec``, the Python port of
  the DIII-D IDL ``modespec`` tool: Mirnov spectrograms, toroidal mode-number
  fits, per-shot mode CSVs. Run via ``tokeye modespec <config.yaml>``.
- :mod:`tokeye.modespec.deep` — placeholder for the next-generation
  single-chord mode-number identification (see its README).
"""

from __future__ import annotations
